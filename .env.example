# Copiar este archivo a .env y completar los valores reales.
# El archivo .env NO debe subirse al repositorio (ya está en .gitignore).

# ── users-api ──────────────────────────────────────────────
USERS_PORT=8080
DB_HOST=mysql
DB_PORT=3306
DB_USER=root
DB_PASSWORD=cambia_esto
DB_NAME=users_db
# Mismo secreto que usan bookings-api y apartments-api para verificar tokens
JWT_SECRET=cambia_esto_por_un_secreto_largo_y_aleatorio

# ── apartments-api ─────────────────────────────────────────
APARTMENTS_PORT=8081
APARTMENTS_MONGODB_URI=mongodb://root:cambia_esto@mongodb:27017/apartments_db?authSource=admin
APARTMENTS_USERS_API_URL=http://users-api:8080
APARTMENTS_RABBITMQ_URL=amqp://admin:cambia_esto@rabbitmq:5672/

# ── bookings-api ───────────────────────────────────────────
BOOKINGS_PORT=8082
BOOKINGS_MONGODB_URI=mongodb://root:cambia_esto@mongodb:27017/bookings_db?authSource=admin
BOOKINGS_USERS_API_URL=http://users-api:8080
BOOKINGS_APARTMENTS_API_URL=http://apartments-api:8081
BOOKINGS_RABBITMQ_URL=amqp://admin:cambia_esto@rabbitmq:5672/

# ── search-api ─────────────────────────────────────────────
SEARCH_PORT=8083
SEARCH_SOLR_URL=http://solr:8983/solr/apartments
SEARCH_MEMCACHED_URL=memcached:11211
SEARCH_APARTMENTS_API_URL=http://apartments-api:8081
SEARCH_RABBITMQ_URL=amqp://admin:cambia_esto@rabbitmq:5672/

# ── notifications-api ──────────────────────────────────────
# Obtener en: resend.com → API Keys
# Free tier: solo envía emails al email del dueño de la cuenta sin verificar dominio
RESEND_API_KEY=
# En producción usar dominio verificado: Reservas <reservas@tu-dominio.com>
FROM_EMAIL=Reservas <onboarding@resend.dev>

# ── Mercado Pago ───────────────────────────────────────────
# Obtener en: mercadopago.com/developers → Credenciales
# Sandbox: usar credenciales del test seller (APP_USR-...)
# Producción: credenciales reales de la cuenta MP
MP_ACCESS_TOKEN=
MP_PUBLIC_KEY=

# URL pública para el webhook de MP (solo en dev con ngrok, apunta al puerto 8082)
# En producción dejar vacío — MP envía el webhook directo al servidor público
NGROK_URL=

# URL del frontend usada en los back_urls de MP Checkout
# En dev: http://localhost:3000 | En prod: https://tu-dominio.com
# Si empieza con https://, auto_return se activa automáticamente
FRONTEND_BASE_URL=http://localhost:3000

# ── frontend ───────────────────────────────────────────────
VITE_API_BASE_URL=http://search-api:8083
VITE_APARTMENTS_API_URL=http://apartments-api:8081
VITE_BOOKINGS_API_URL=http://bookings-api:8082

# ── infraestructura ────────────────────────────────────────
MYSQL_ROOT_PASSWORD=cambia_esto
MONGO_INITDB_ROOT_USERNAME=root
MONGO_INITDB_ROOT_PASSWORD=cambia_esto
RABBITMQ_DEFAULT_USER=admin
RABBITMQ_DEFAULT_PASS=cambia_esto
